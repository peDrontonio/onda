# Python dependencies for the Braco RRRPR control/simulation (peter_corke/scripts).
# Tested with Python 3.10 inside an isolated virtual environment.
#
#   python3 -m venv .venv        # (or: python3 -m virtualenv .venv)
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
# roboticstoolbox-python pulls in spatialmath-python, pgraph-python, ansitable
# and rtb-data automatically; they are pinned here for reproducibility.

numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
roboticstoolbox-python==1.3.0
spatialmath-python==1.1.16
pgraph-python==0.6.4
ansitable==0.11.5
rtb-data==1.1.0
